```markdown
# TELEGRAM QUOTES BOT - ДОКУМЕНТАЦИЯ ПРОЕКТА

Бот для автоматической публикации цитат в Telegram канал.

---

## 📁 ФАЙЛОВАЯ СТРУКТУРА

```bash
.
├── autoposting
│   ├── README.md
│   └── tg
│       ├── docker-compose.yml
│       ├── logs
│       │   └── bot.log
│       └── quotes
│           ├── data
│           │   ├── published_history.json
│           │   ├── quotes365.json
│           │   └── quotes-example.json
│           ├── Dockerfile
│           ├── get-real-digest.sh
│           ├── .env
│           ├── .env.example
│           ├── requirements.txt
│           └── src
│               ├── bot.py
│               ├── config.py
│               ├── history.py
│               └── utils.py
└── README.md
└── notes.md
└── .gitignore
```

---

## 🔐 ХРАНЕНИЕ СЕКРЕТОВ

Секреты хранятся в менеджере паролей.  
Конфиги синхронизируются через облако или локально через симлинк (чтобы не попадали в git).  
Чувствительные данные добавлены в `.gitignore`:

```bash
tg
├── bot.log
└── quotes
    ├── .env
    └── data
        ├── quotes365.json
        └── published_history.json
```

---

## 🚀 УСТАНОВКА

### 1. Клонирование репозитория

```bash
git clone https://github.com/kadannrheim/small-projects.git
```

### 2. Создание файла .env

```bash
cp .env.example .env
```

### 3. Заполнение .env своими значениями

- Получите токен бота у [@BotFather](https://t.me/BotFather)
- Укажите ID вашего канала (например, `@mychannel`)

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

---

## 🏃 ЗАПУСК

В проекте используется основной запуск через `docker-compose`.  
Дополнительные способы указаны для отладки.

### Локально

```bash
python src/bot.py
```

### Docker (рекомендуемый способ)

```bash
docker-compose up -d
```

### Docker без docker-compose

```bash
docker build -t quotes-bot .
docker run -d --name quotes-bot \
  -e BOT_TOKEN="your_token" \
  -e CHANNEL_ID="@your_channel" \
  quotes-bot
```

---

## ⚙️ ОСНОВНЫЕ НАСТРОЙКИ В .env

```
BOT_TOKEN                 # Токен бота от @BotFather
CHANNEL_ID                # ID канала (начинается с @)
BASE_HOUR                 # Час публикации (по умолчанию 9)
BASE_MINUTE               # Минута публикации (по умолчанию 0)
RANDOM_RANGE_MINUTES      # Случайное смещение в минутах (по умолчанию ±30)
```

---

## 📝 ДОБАВЛЕНИЕ ЦИТАТ

Добавляйте цитаты в файл `data/quotes.json` в формате:

```json
[
  {
    "text": "Текст цитаты",
    "author": "Автор",
    "hashtag": "#тег"
  }
]
```

---

## 🔄 РАЗВЁРТЫВАНИЕ

```bash
git init
git clone https://github.com/kadannrheim/small-projects.git
```

---

## 🔗 ХРАНЕНИЕ ПЕРСОНАЛИФИЦИРОВАННОЙ ИНФОРМАЦИИ

Для тех, кто разворачивает на своём ПК.  
Данные хранятся отдельно в локальной/облачной папке, создаётся симлинк для добавления цитат в проект.

### Хранение цитат (Windows симлинк)

```batch
:: Запустите cmd от имени Администратора
:: (Win + R → cmd → Ctrl+Shift+Enter)

cd [локальный путь]\git\small-projects\autoposting\tg\quotes\data
mklink quotes365.json "[локальный путь]\quotes\quotes365.json"
```

### Хранение конфига .env (Windows симлинк)

```batch
:: Запустите cmd от имени Администратора

cd [локальный путь]\git\small-projects\autoposting\tg\quotes
mklink .env "[локальный путь]\quotes\config\telegram-quotes.env"
```

Имена симлинков: `quotes365.json` и `.env`

---

## 🔖 ВЕРСИОННОСТЬ

### Ветвление

```
main (стабильная)
└── dev (разработка, после тестов идёт в main)
    ├── feature/*
    └── fix/*
```

### Описание наименования коммитов

| Тип       | Когда использовать                          |
|-----------|---------------------------------------------|
| `feat`    | Новая функциональность                      |
| `fix`     | Исправление бага                            |
| `docs`    | Изменения в документации                    |
| `style`   | Форматирование, отступы (не влияет на код)  |
| `refactor`| Рефакторинг кода                            |
| `perf`    | Улучшение производительности                |
| `test`    | Добавление или исправление тестов           |
| `chore`   | Обновление задач, настройка                 |
| `build`   | Сборка, зависимости                         |
| `ci`      | Настройка CI/CD                             |

Если нужно подробнее — добавляется тело коммита:

```bash
git commit -m "feat: add password reset functionality

- Add reset password endpoint
- Send email with reset link
- Add validation for token expiration"
```

### Указание тэгов

```bash
git tag -a v1.0.0 -m "Release version 1.0.0"
```

---

## КОНЕЦ ФАЙЛА
```