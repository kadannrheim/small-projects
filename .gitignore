# =============================================================================
# .gitignore - КАКИЕ ФАЙЛЫ НЕ ПОПАДАЮТ В GIT
# =============================================================================
#
# Этот файл определяет, какие файлы и директории Git должен игнорировать.
# Важно для:
#   - Безопасности (секреты, токены)
#   - Чистоты репозитория (временные файлы)
#   - Кроссплатформенности (системные файлы)
#
# Паттерны:
#   * - любая строка
#   ? - один любой символ
#   [abc] - один из символов
#   ** - любая вложенная директория
#
# =============================================================================



# =============================================================================
# 🔐 РАЗДЕЛ 1: СЕКРЕТЫ И КОНФИДЕНЦИАЛЬНЫЕ ДАННЫЕ
# =============================================================================
# Эти файлы содержат токены, пароли и другую чувствительную информацию.
# НИКОГДА не коммитьте их в Git!

# Файлы окружения (содержат токены ботов)
.env                    # Основной файл с секретами
.env.local              # Локальные переопределения
.env.*.local            # Все локальные варианты
*.env                   # Любые env файлы

# Директории с секретами
secrets/                # Директория с секретами
keys/                   # SSH ключи и сертификаты

# Файлы ключей и сертификатов
*.key                   # Приватные ключи
*.pem                   # PEM сертификаты (если приватные)
*.crt                   # CRT сертификаты (если приватные)
*.pub                   # Публичные ключи (обычно безопасно, но лучше игнорировать)

# Terraform секреты
*.tfvars                # Файлы с переменными Terraform
terraform.tfvars        # Основной файл переменных
*.auto.tfvars           # Автоматические переменные

# Файлы с паролями
.passwords              # Любые файлы с паролями
.credentials            # Файлы с учетными данными
auth.json               # Файлы аутентификации



# =============================================================================
# 📦 РАЗДЕЛ 2: ПАКЕТЫ И АРХИВЫ
# =============================================================================
# Скомпилированные файлы и архивы не нужно хранить в Git

# Java байт-код
*.class                 # Скомпилированные Java классы
*.jar                   # Java архивы
*.war                   # Web архивы
*.ear                   # Enterprise архивы

# Другие языки
*.o                     # Объектные файлы (C/C++)
*.so                    # Библиотеки (Linux)
*.dylib                 # Библиотеки (macOS)
*.dll                   # Библиотеки (Windows)
*.exe                   # Исполняемые файлы
*.out                   # Выходные файлы

# Архивы (обычно это бинарные зависимости)
*.tar                   # Tarball архивы
*.tgz                   # Сжатые архивы
*.gz                    # Gzip архивы
*.zip                   # Zip архивы
*.7z                    # 7-Zip архивы
*.rar                   # RAR архивы
*.deb                   # Debian пакеты
*.rpm                   # RPM пакеты



# =============================================================================
# 💻 РАЗДЕЛ 3: IDE И РЕДАКТОРЫ
# =============================================================================
# Файлы конфигурации IDE и редакторов (индивидуальны для каждого разработчика)

# VS Code
.vscode/                # Вся директория настроек VS Code
.vscode/settings.json   # Настройки проекта (если содержат пути)
.vscode/workspace.code-workspace

# IntelliJ IDEA (и другие JetBrains IDE)
.idea/                  # Директория проекта IntelliJ
*.iml                   # Модули IntelliJ
*.iws                   # Рабочие пространства
*.ipr                   # Проекты

# Eclipse
.classpath              # Java classpath
.project                # Проект Eclipse
.settings/              # Настройки Eclipse

# Vim/Neovim
*.swp                   # Swap файлы Vim
*.swo                   # Другие swap файлы
*.swn                   # Третьи swap файлы
*.~                     # Бэкапы
*.tmp                   # Временные файлы
Session.vim             # Сессии Vim
.netrwhist              # История сети Vim

# Emacs
*~                      # Бэкапы Emacs
\#*#                    # Автосохранения Emacs
.\#*                    # Файлы блокировки
*.elc                   # Скомпилированный Emacs Lisp



# =============================================================================
# 🐍 РАЗДЕЛ 4: PYTHON
# =============================================================================
# Файлы, специфичные для Python-проектов

# Кэш байт-кода
__pycache__/            # Директория с кэшированным байт-кодом
*.py[cod]               # .pyc, .pyo, .pyd файлы
*.pyc                   # Скомпилированный Python
*.pyo                   # Оптимизированный Python (устарело)
*.pyd                   # Python расширения (Windows)

# Виртуальные окружения
.env/                   # Виртуальное окружение (если в .env)
.venv/                  # Стандартное имя для виртуального окружения
venv/                   # Другое стандартное имя
env/                    # Еще одно имя
ENV/                    # Верхний регистр
env.bak/                # Бэкапы окружений
venv.bak/               # Бэкапы окружений
.python-version         # Версия Python (pyenv)

# Файлы дистрибуции
build/                  # Директория сборки
dist/                   # Директория дистрибуции
*.egg-info/             # Информация об egg
*.egg                   # Egg файлы
MANIFEST                # Манифест сборки

# Инструменты разработки
.pytest_cache/          # Кэш pytest
.mypy_cache/            # Кэш mypy
.ruff_cache/            # Кэш ruff (линтер)
.coverage               # Данные покрытия тестами
.coverage.*             # Детальные данные покрытия
htmlcov/                # HTML отчет покрытия
*.cover                 # Покрытие по файлам
.tox/                   # Tox директория
nosetests.xml           # Результаты nose тестов
coverage.xml            # Отчет покрытия в XML

# Jupyter Notebooks (если используются)
.ipynb_checkpoints/     # Контрольные точки Jupyter
*.ipynb                 # Сам ноутбук (если не нужен в репозитории)



# =============================================================================
# 🐳 РАЗДЕЛ 5: DOCKER
# =============================================================================
# Файлы, связанные с Docker

# Docker данные
*docker*/data/          # Любые данные в docker директориях
docker-compose.override.yml  # Локальные переопределения compose
docker-compose.*.yml    # Все compose файлы, кроме основного

# Локальные Docker файлы
.dockerignore           # Обычно нужно в репозитории, но можно игнорировать
Dockerfile.dev          # Dev версии Dockerfile
Dockerfile.prod         # Prod версии (если есть отдельно)



# =============================================================================
# 📝 РАЗДЕЛ 6: ЛОГИ И ДИАГНОСТИКА
# =============================================================================
# Лог-файлы и другие временные диагностические данные

# Лог файлы
*.log                   # Все лог файлы
logs/                   # Директория с логами
bot.log                 # Лог бота
crash.log               # Лог падений
error.log               # Лог ошибок
access.log              # Лог доступа
debug.log               # Отладочный лог

# Дампы и профилирование
*.pid                   # PID файлы
*.seed                  # Seed файлы
*.pid.lock              # Блокировки PID
npm-debug.log*          # Логи npm
yarn-debug.log*         # Логи yarn
yarn-error.log*         # Ошибки yarn

# Дампы памяти
*.dmp                   # Дампы памяти
*.heapdump              # Дампы кучи
*.core                  # Core дампы



# =============================================================================
# 🗄️ РАЗДЕЛ 7: РЕЗЕРВНЫЕ КОПИИ И ВРЕМЕННЫЕ ФАЙЛЫ
# =============================================================================
# Бэкапы и временные файлы, создаваемые редакторами и утилитами

# Бэкапы (общие паттерны)
*_backup*               # Любые файлы с _backup в имени
*_old*                  # Старые версии
*.bak                   # Бэкапы с расширением .bak
*.backup                # Бэкапы с расширением .backup
*.orig                  # Оригинальные файлы (после merge)
*.old                   # Старые файлы
*.new                   # Новые файлы (в процессе)

# Временные файлы
*.tmp                   # Временные файлы
*.temp                  # Другое расширение для временных
*.swp                   # Swap файлы (уже выше)
*.swo                   # Swap файлы
tmp/                    # Директория с временными файлами
temp/                   # Другая временная директория

# Конкретные бэкапы для этого проекта
bot.py_backup*          # Бэкапы bot.py
config.py_backup*       # Бэкапы config.py
quotes.txt_backup       # Бэкапы цитат (если есть)



# =============================================================================
# 🖥️ РАЗДЕЛ 8: ОПЕРАЦИОННЫЕ СИСТЕМЫ
# =============================================================================
# Системные файлы, создаваемые ОС

# macOS
.DS_Store               # Метаданные Finder
.DS_Store?              # Разные версии .DS_Store
._*                     # Файлы ресурсов (AppleDouble)
.Spotlight-V100         # Индекс Spotlight
.Trashes                # Корзина
.AppleDouble            # AppleDouble формат
.LSOverride             # Переопределения Launch Services

# Windows
Thumbs.db               # Кэш миниатюр
Thumbs.db:encryptable   # Зашифрованный кэш
ehthumbs.db             # Кэш для видео
Desktop.ini             # Настройки папки
$RECYCLE.BIN/           # Корзина
*.lnk                   # Ярлыки
*.url                   # Интернет-ярлыки

# Linux
*~                      # Резервные копии (уже выше)
.*.swp                  # Скрытые swap файлы
.directory              # Настройки директорий (KDE)



# =============================================================================
# 🧪 РАЗДЕЛ 9: ТЕСТИРОВАНИЕ
# =============================================================================
# Файлы, создаваемые при тестировании

# Отчеты тестов
test-reports/           # Директория с отчетами
test-results/           # Результаты тестов
junit/                  # JUnit отчеты
*.junit.xml             # XML отчеты JUnit

# Кэш тестов
.pytest_cache/          # Кэш pytest
.cache/                 # Общий кэш
.coverage               # Покрытие (уже выше)
htmlcov/                # HTML покрытие (уже выше)

# Mock данные
.mocks/                 # Mock объекты для тестов
fixtures/               # Тестовые фикстуры (если генерируются)



# =============================================================================
# 🎯 РАЗДЕЛ 10: СПЕЦИФИЧНЫЕ ДЛЯ ПРОЕКТА
# =============================================================================
# Файлы, специфичные именно для этого проекта

# Конфигурационные файлы для разных окружений
telegram-quotes.env     # Env файл для этого бота
news-poster.env         # Env для другого бота (если есть)

# Файлы данных (которые генерируются, а не исходные)
quotes365.json          # Основной файл с цитатами
published_history.json  # История публикаций (СОХРАНЯТЬ!)
*.history.json          # Любые файлы истории


# =============================================================================
# 📚 РАЗДЕЛ 11: ДОКУМЕНТАЦИЯ (ОПЦИОНАЛЬНО)
# =============================================================================
# Некоторые автоматически сгенерированные документы

/docs/_build/           # Сгенерированная документация
/docs/build/            # Другая директория сборки
/site/                  # Сайт с документацией
*.pdf                   # PDF документы (если генерируются)
*.epub                  # EPUB документы



# =============================================================================
# 🚀 РАЗДЕЛ 12: CI/CD (НЕПРЕРЫВНАЯ ИНТЕГРАЦИЯ)
# =============================================================================
# Файлы, создаваемые CI системами

# GitHub Actions
.github/cache/          # Кэш GitHub Actions
.github/tmp/            # Временные файлы

# GitLab CI
.tmp/                   # Временные файлы
.cache/                 # Кэш

# Jenkins
workspace/              # Рабочая директория Jenkins
@tmp/                   # Временные файлы Jenkins



# =============================================================================
# ✅ ИСКЛЮЧЕНИЯ ИЗ ПРАВИЛ
# =============================================================================
# Иногда нужно явно разрешить некоторые файлы, даже если они подпадают под
# паттерны выше. Используйте ! для исключений.

# Например, если нужен конкретный .env.example в репозитории:
!.env.example           # Не игнорировать пример .env файла

# Если нужны примеры конфигурации:
!.env.*.example         # Все примеры env файлов
!config.example.yaml    # Пример конфига YAML

# Если нужны пустые директории (Git не хранит пустые директории,
# но можно добавить .gitkeep файлы)
!.gitkeep               # Сохранять пустые директории

# Примеры данных
!quotes-example.json    # Пример файла с цитатами
!data/example/          # Директория с примерами данных

# =============================================================================
# КОНЕЦ ФАЙЛА .gitignore
# =============================================================================